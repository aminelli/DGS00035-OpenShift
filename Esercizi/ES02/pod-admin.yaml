# OpenShift  version: 4.20.15
# Kubernetes version: v1.33.6
apiVersion: v1
kind: Pod
metadata:
  name: pod-oc-cli
  namespace: prj-antonio-minelli
  labels:
    app: pod-oc-cli
    env: debug
spec:
  containers:
  - name: cnt-oc-cli
    image: aminelli/cli-oc:v2
    command: ["/bin/bash", "-c", "sleep infinity"]
    securityContext:
      privileged: true